   public static int PrintCharactersInString(string word)
        {
            for (int i = 0; i < word.Length; i++) {

                Console.WriteLine(word[i]);
            
            }

            return -1;
        }